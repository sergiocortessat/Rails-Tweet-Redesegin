<%= form_for model:@comments, url: comments_path do |form| %>
      <%= form.text_field :content, class: 'form-control mb-2', placeholder: 'Add new Comment' %>
      <%= form.submit 'Comment', class: 'btn btn-secondary'%>
    <% end %>